# =============================================================================
# RIPPLE - GPU Requirements (CUDA Mode)
# =============================================================================
# Core dependencies for GPU mode. Based on the cuda-env environment.
# Install with: pip install -r requirements-gpu.txt
# 
# For exact reproducibility, use: pip install -r requirements-frozen.txt
#
# Prerequisites:
#   - NVIDIA GPU with CUDA Compute Capability 3.5+
#   - NVIDIA Driver 450.80.02+ (Linux) or 452.39+ (Windows)
# =============================================================================

# Core scientific computing
numpy>=2.0.0
scipy>=1.10.0
pandas>=2.0.0

# Image processing
tifffile>=2023.2.0
imageio>=2.28.0
mediapy>=1.2.0
opencv-python>=4.7.0
scikit-image>=0.20.0
pillow>=10.0.0

# PyTorch with CUDA (comes with CUDA runtime bundled)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# PyTorch ecosystem
pytorch-lightning>=2.0.0
lightning>=2.0.0
torchmetrics>=1.0.0
einops>=0.6.0

# TensorFlow (for LocoTrack compatibility)
tensorflow>=2.15.0
tensorflow-addons>=0.23.0
tensorflow-datasets>=4.9.0
tensorflow-graphics>=2021.12.0
keras>=3.0.0

# Particle tracking
trackpy>=0.6.0

# Data handling
h5py>=3.8.0
pyarrow>=12.0.0

# System utilities
psutil>=5.9.0
tqdm>=4.65.0
requests>=2.28.0

# Visualization
matplotlib>=3.7.0

# Utilities
pyyaml>=6.0
click>=8.0.0
rich>=13.0.0
